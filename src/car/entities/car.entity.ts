export class Car {
  id: number;           // Identificador único del coche (auto incremental)
  plate: string;        // Placa del coche
  release_date: Date;   // Fecha de lanzamiento del coche
  is_available: boolean; // Estado de disponibilidad del coche (si está disponible o no)
  price: number;        // Precio del coche
  comunidad_id: number; // ID de la comunidad a la que está vinculado el coche
}

